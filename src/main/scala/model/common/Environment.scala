package model.common

case class Environment(temperature: Int,
                       buttefly: Int,
                       plant: Int,
                       predator: Int,
                       days: Int)
